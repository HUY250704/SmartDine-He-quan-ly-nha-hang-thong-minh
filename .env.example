# SmartDine - Root Environment Variables
#
# Copy this file to .env and adjust values as needed.
# This monorepo uses npm workspaces with concurrently.
# See backend/.env.example and frontend/.env.example for workspace-specific config.

# Port for the concurrently dev runner (informational, not used by code)
PORT=3000
